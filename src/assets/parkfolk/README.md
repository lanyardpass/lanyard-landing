# Parkfolk figures (web copies)

Copied from `brand/crowd-figures/` (lanyard-docs), which is GENERATED from
`CrowdFigures.swift` by `tools/dump-shape-svgs.py` — the Swift is the source
of truth. Re-copy after any figure change lands. Unit-square viewBox; the
consumer sets width/height from the app registry aspect/relHeight and
`preserveAspectRatio="none"` (see CrowdPlaza.astro).
