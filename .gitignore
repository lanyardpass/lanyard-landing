# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / OS noise
*.bak
*.tmp
*.swp
.vscode/
.idea/
.obsidian/

# Node (future-proofing if we ever add a build step)
node_modules/
npm-debug.log
yarn-debug.log
yarn-error.log

# Netlify CLI (if we ever install it locally)
.netlify/

# Unused local asset — 4MB source logo not referenced by any HTML
# If you want this tracked, remove the line below
logo.png
